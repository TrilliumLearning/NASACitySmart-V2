<!-- views/login.ejs -->
<!doctype html>
<html>
<head>
	<title>CitySmart - Successful Request</title>
    <script src="../3rdPartyLibs/jquery-3.3.1.min.js"></script>
</head>
<body>
<div>
    <p> Your request is success</p>
    <p> You will be redirected to the login page in <span id="countdowntimer">3 </span> Seconds</p>
</div>
<script>
    $(document).ready(function(){
        $(function () {
            let timeleft = 3;
            let downloadTimer = setInterval(function () {
                timeleft--;
                document.getElementById("countdowntimer").textContent = timeleft;
                if (timeleft <= 0) {
                    clearInterval(downloadTimer);
                    window.location.replace('/login');
                }
            }, 1000);
        });
    });
</script>
</body>
</html>
